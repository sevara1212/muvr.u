rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents during development
    // IMPORTANT: Change these rules before deploying to production
    match /{document=**} {
      allow read, write;
    }
  }
} 